###### 第一步：安装Homebrew
1.检查是否已经安装
终端执行：brew --version
如果已经安装则跳过

2.安装Homebrew
终端执行：/bin/bash -c "$(curl -fsSL https://gitee.com/ineo6/homebrew-install/raw/master/install.sh)"

###### 第二步：清理并重新安装open-jdk21 cocoapods ruby，并自动同步环境，避免环境冲突
1.完全退出android studio
2.终端执行以下命令：
/bin/bash -c "
brew uninstall --force cocoapods ruby openjdk@21;
sudo gem uninstall cocoapods -a -x;
brew install ruby openjdk@21 cocoapods;
CFG_STR='
export JAVA_HOME=\"\$(/usr/libexec/java_home -v 21)\"
export PATH=\"/opt/homebrew/opt/openjdk@21/bin:\$PATH\"
export PATH=\"/opt/homebrew/opt/ruby/bin:\$PATH\"
export PATH=\"/opt/homebrew/bin:\$PATH\"
';
grep -q \"KMP Environment Setup\" ~/.zshrc || echo \"\$CFG_STR\" >> ~/.zshrc;
source ~/.zshrc;
sudo ln -sfn /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-21.jdk;
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer;
sudo xcodebuild -license accept;
echo '------------------------------------';
echo '✅ 执行完毕！请检查以下输出：';
java -version;
pod --version;
which ruby;
echo '------------------------------------';
echo '⚠  请务必【完全退出】Android Studio 重新打开！';
"

###### 第三步：Android studio安装Kotlin Multiplatform插件
1.如果已经安装，则先更新到最新，该插件须保持最新，否则可能会和Xcode编译工具链不兼容，出现一些奇奇怪怪的问题
2.Andriod studio也同样须更新到最新，否则可能会和Xcode编译工具链不兼容，出现一些奇奇怪怪的问题

###### 第四步：Android studio打开项目并等待初始化完成
###### 第五步：为gradlew添加可执行权限
方法一：终端执行 chmod -x gradlew
方法二：在android studio中，点击右侧飞机图标，如果没有可执行权限，ide会出现一个快捷按钮，点击即可完成授权

###### 第六步：安装cocoapods依赖库（如果有添加新的则需要手动再次进行安装）
1.终端切换到iosApp目录
2.终端执行：pod install

###### 第七步：Xcode + Android Studio构建环境同步配置
详见RunConfiguration.md文件，或对ai说“IDE环境同步”



