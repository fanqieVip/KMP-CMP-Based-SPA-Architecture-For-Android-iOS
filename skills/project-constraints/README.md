# 当前项目约束

本文档目录用于存放当前项目专属约束。

## 约束沉淀边界

- 当 AI 发现需要打补丁或沉淀新规则时，除非开发者明确说明写入当前项目约束，否则不得写入本目录。
- 未获明确授权时，AI 只能在对话中提示发现了可沉淀规则，并等待开发者确认。
- 本目录仅记录当前项目专属约束，不替代 `skills/workflows/`、`skills/standards/`、`skills/knowledge/` 或 `skills/patches/` 的既有职责。
